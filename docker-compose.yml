services:
  equity-voting-backend:
    container_name: equity-voting-backend
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5010:5010"
